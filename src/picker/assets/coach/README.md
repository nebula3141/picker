# First-run coach-mark images

Drop three screenshots here, named exactly:

- `1.jpg` — the folder browser (a folder of photo tiles / album grid)
- `2.jpg` — the image viewer mid-sort (right-click Selection menu open, or the
  status bar showing `Copy → …` with shortcut hints)
- `3.jpg` — the `?` shortcuts panel over a photo, or the `Ctrl+F` search bar

Recommended: **landscape, ~16:10, at least ~1000px wide.** They render as a
rounded banner at the top of each welcome card.

Missing images are handled gracefully — a card simply shows its text with no
banner — so the welcome screen works even before you add them.
